import 'package:get/get.dart';

/// `NotificationController` is registered globally via `AppBindings`, so this
/// binding has no per-route dependencies of its own.
class NotificationBinding implements Bindings {
  @override
  void dependencies() {}
}
